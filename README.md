# AI-Medical-Chat-Bot

# step 1: clone the repo
```
git clone <your-repo>
```

# step 2: create a virtual env
```
python -m venv <virtualenv>
```

# step 3 : Activate virtual env
```
scripts\activate = your env
```

# step 4 : install the requirements
```
cmd : pip install -r requirements.txt
```

